.row.alert_row
  if req.settings_page
    .top
  else
    .help_overlay.active
      .bg
      .center_column
        .button_hide Hide Help
        .button_show Show Help
        .dialog.right.two
          .text Email Address
        .dialog.right.five
          .text A Website
    .top
      .center_column
        h1.two 
          .one Settings
          .two Set links and adjust notifications
        .images
          img.text(src='/images/icons/alert_333333.png')
  .clear
  .mid_spacer
  .mid
    .center_column
      .set_alerts
        .row_bg
        .row_content
          .field
            h2 Login
          .clear
          .field
            p We will send your order confirmation to this email address.
            p You may use a social network for login instead.
          .field
            .input
              label E-mail
              input.order_email.required(placeholder='my@email.com',pattern='.+@.+\\..+')
              .symbol *
            .clear
            .spacer
            .input
              label &nbsp;
              ul.social_links
                li.facebook F
                li.twitter.shift t
                li.linkedin l
                li.google g
  .clear
  .mid_spacer
  .mid
    .center_column
      .set_links
        .row_bg
        .row_content
          .field
            h2 Link
          .clear
          .field
            p Where they go, when they scan.
          .field
            .link_editor
              .input
                label URL
                input.set_link.required(placeholder='mysite.com',pattern='.+\\..+')
                .symbol *
            .link_items
  .clear
  .mid_spacer
  .mid
    .center_column
      .set_alerts
        .row_bg
        .row_content
          .field
            h2 Alert
          .clear
          .field
            p When they scan, we can send you an email or an SMS text message indicating which specific card was scanned.
          .field
            .input
              label Send To
              .toggle.order_alerts
                .bg
                  .option.active(alerts='off') None
                  .option(alerts='emails') Email
                  .option(alerts='texts') Phone
            .clear
            .spacer
            .input
              label Phone
              input.order_phone.optional(placeholder='555 555 5555',pattern='.*[0-9].*[0-9].*[0-9].*[0-9].*[0-9].*[0-9].*[0-9].*[0-9].*[0-9].*[0-9].*')
              .symbol
            .clear
            .spacer
      .clear
    .clear
  .clear
.clear